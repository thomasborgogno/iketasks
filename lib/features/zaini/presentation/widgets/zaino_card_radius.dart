import 'package:flutter/material.dart';

/// Corner radius shared by every card-like surface in the zaino item list —
/// category headers, item tiles — so a category group reads as one
/// continuous rounded card.
const cardRadius = Radius.circular(16);
